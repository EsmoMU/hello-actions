name: first

on: 
    push:
        branches: market_place_workflow

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: Check out the code
              uses: actions/checkout@v2
            - name: Python Syntax Checker
              uses: cclauss/Find-Python-syntax-errors-action@v0.2.0
